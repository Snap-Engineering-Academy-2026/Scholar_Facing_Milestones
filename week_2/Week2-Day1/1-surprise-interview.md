# SURPRISE INTERVIEW!


## Third Most Frequent Element

The frequency of an element is the number of times it occurs in an array.
You are given a character array ```letters```.

Return the *third most frequent occuring element*.

### Example 1
```
Input: letters = ['a', 'b', 'a', 'c', 'd', 'a', 'b', 'c']
Output: 'c'
```

### Example 2
```
Input: letters = ['z', 'z', 'z', 'y', 'y', 'y', 'x']
Output: 'x'
```

### Example 3
```
Input: letters = ['f', 'g', 'h', 'i']
Output: null
```



```js
/**
 * @param {char[]} letters
 * @return {string}
 */


const thirdMostFrequentElement = function(letters) {
    
};
```